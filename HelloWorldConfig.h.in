#ifndef INCLUDE_GUARD
#define INCLUDE_GUARD

#define HelloWorld_NAME "@HelloWorld_NAME@"
#define HelloWorld_VER "@HelloWorld_VERSION@"
#define HelloWorld_VERSION_MAJOR "@HelloWorld_VERSION_MAJOR@"
#define HelloWorld_VERSION_MINOR "@HelloWorld_VERSION_MINOR@"
#define HelloWorld_VERSION_PATCH "@HelloWorld_VERSION_PATCH@"

#cmakedefine USE_ADDER  //Ignore the red underline, this does not generate an error but I do not know how to get rid of the 
                        //red underline without turning off syntax help, which I would like to avoid doing so..

// #ifdef USE_ADDER
//     #include<adder.h>
// #endif

#endif